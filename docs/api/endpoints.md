# AllEnergy REST endpoints

Base path: `/api/allenergy/v1/`

Auth: API key via `X-API-Key` header. Key is stored in `ir.config_parameter` under `allenergy.api_key`.

Endpoints will be documented here as they are implemented in Phase 2.
